# Babe House — กฎการทำงานกับโปรเจคนี้

## 🚨 กฎเหล็กเรื่องการขึ้นเว็บ (อ่านก่อนทุกครั้ง)

**เว็บลูกค้ากับหลังบ้านคือเซิร์ฟเวอร์ตัวเดียวกัน ฐานข้อมูลชุดเดียวกัน**
`/admin` และ `/projects` ไม่ได้แยกจาก `/`, `/form`, `/account` — deploy ครั้งเดียวรีสตาร์ททั้งหมด
เพราะฉะนั้น **"งานหลังบ้าน" ไม่มีอยู่จริงในแง่ความเสี่ยง** ทุก deploy คือความเสี่ยงกับลูกค้าที่กำลังจ่ายเงินอยู่

### ต้องผ่าน staging ก่อนเสมอ
```bash
cd web && npm run build
git push origin main:staging     # ทดสอบที่ babe-house-v2-staging.up.railway.app ก่อน
# ผ่านครบแล้วเท่านั้น ค่อย:
git push origin main
```

**ยกเว้นได้กรณีเดียว:** ของพังอยู่จริงตอนนั้นและลูกค้าได้รับผลกระทบ

### ห้ามขึ้น prod ตรงเด็ดขาด ถ้างานนั้น
- แตะ schema ฐานข้อมูล (`server/db.js`)
- แตะเรื่องเงิน / Stripe / ราคา
- เพิ่มฟีเจอร์ใหม่ที่ลูกค้ายังไม่ได้ขอให้รีบ
- เป็นการทดลอง/ตัวอย่าง

### ห้าม deploy ในเวลาทำการ
ลูกค้าเปิดหน้าค้างไว้ → ไฟล์ .js เปลี่ยนชื่อ → หน้าพัง (เคยเกิดจริง 31 ก.ค.)
⛔ **ห้ามคิดว่า "กลางคืนไม่มีคนใช้"** — คิมยืนยันว่าลูกค้าใช้เว็บตอนกลางคืนเยอะมาก ตี 3 ยังมีคนซื้อ
และถ้าพังตอนดึก แอดมินหลับหมด ไม่มีใครแก้ ลูกค้าซื้อไม่ได้แล้วพรุ่งนี้อาจไม่กลับมา (เคยเกิดจริง 2 ส.ค. ดับ 25 นาที)

## 🎮 สนามเด็กเล่น — ทดสอบในเครื่อง ไม่แตะเว็บจริง

**ลูกค้าใช้เว็บตลอดเวลา ตี 3 ยังมีคนซื้อ (คิมยืนยัน 2 ส.ค.) — ไม่มีเวลาไหน "ปลอดภัย" ให้ลองของบน prod**

```bash
npm run play          # เปิดที่ http://localhost:3000
npm run play:reset    # ล้างข้อมูลเริ่มใหม่
```

- ฐานข้อมูลอยู่ในไฟล์ `.localdb/` บนเครื่อง — **ไม่ต่อเน็ต ไม่แตะฐานข้อมูลลูกค้า** (`DATABASE_URL` ไม่ถูกใช้เลย)
- ใช้ PGlite = Postgres ตัวจริงที่คอมไพล์ให้รันในตัว Node ไม่ต้องติดตั้ง Postgres/Docker
- AI = โหมด fallback (ไม่เผาโควตา) · จ่ายเงิน = mock · อีเมล = ไม่ส่งจริง
- หลังบ้านเข้าด้วยรหัส `play` ที่ `/admin` และ `/projects`
- พังยังไงก็ไม่กระทบใคร → `npm run play:reset` แล้วเริ่มใหม่

**ลำดับที่ถูกต้องของทุกงาน:** สนามเด็กเล่น → staging → (คิมอนุมัติ) → prod

สนามเด็กเล่นจับบั๊กที่ prod มองไม่เห็นได้ด้วย — เช่น `ALTER TABLE` มาก่อน `CREATE TABLE` (เจอจริงวันแรกที่ทำ)

## 🛟 กฎการเขียนโค้ดฝั่งเซิร์ฟเวอร์

- **ห้ามมี `process.exit()` ใน startup path** — ต่อ DB ไม่ทันชั่วคราวต้องลองใหม่ ไม่ใช่ฆ่าตัวเอง
  ปัจจุบัน: `app.listen()` ก่อน → `connectDbWithRetry()` (backoff 2s→30s ไม่ยอมแพ้) → seed แต่ละตัวห่อ try/catch
- `server/db.js`: `CREATE TABLE` ให้ครบก่อน `ALTER`/`UPDATE` เสมอ (กัน DB เปล่าพัง)
- Zod schema **ตัด key ที่ไม่ได้ประกาศทิ้งเงียบๆ** — เพิ่มฟิลด์ใน payload ต้องเพิ่มใน `CheckoutSchema`/`GenSchema` ด้วย
- เส้นทาง API ต้องประกาศ**ก่อน** `app.get("*")` ของ SPA ไม่งั้นถูกหน้าเว็บกลืน
- เมลหลังบ้านทุกฉบับใช้ `OPS_EMAIL` (→ babehouse.work@gmail.com) ห้ามส่งเข้า babehouse555 เพราะทีมเห็น

## 🛡️ ด่านตรวจก่อนขึ้นเว็บ (ติดตั้ง 2 ส.ค. หลังล่ม 2 ครั้งในวันเดียว)

**ห้ามทดสอบจากโฟลเดอร์เครื่องตัวเอง — ต้องทดสอบจากต้นไม้ของคอมมิตที่จะขึ้นจริง**
(เหตุล่มครั้งที่ 2: ไฟล์บนสายที่ push ขาด `seed-playground.js` แต่เครื่องเรามีไฟล์นั้น เลยทดสอบผ่าน)

```bash
bash scripts/preflight.sh <commit>   # ตรวจ: ไวยากรณ์ · import ครบ · บูตติดจริง
```

- **pre-push hook รันด่านนี้เองอัตโนมัติ** ทุกครั้งที่ push ไป main/staging — ไม่ผ่าน = push ไม่ได้
- hook อยู่ที่ `.git/hooks/pre-push` (โคลนใหม่ต้องติดตั้งซ้ำ: `cp scripts/pre-push.hook .git/hooks/pre-push && chmod +x .git/hooks/pre-push`)
- `--no-verify` ใช้ได้เฉพาะไฟไหม้จริงเท่านั้น
- **ห้าม `git checkout สายอื่น -- ไฟล์` ลงสายที่จะ deploy** — ไฟล์อาจเรียกหาเพื่อนบ้านที่ไม่มีในสายนั้น ให้แก้บนสายนั้นตรงๆ แล้วรัน preflight

**Railway Healthcheck (คิมตั้งครั้งเดียว):** Service → Settings → Deploy → Healthcheck Path = `/api/health`
→ ตัวใหม่พังบูตไม่ขึ้น Railway จะไม่สลับ traffic และคงตัวเก่าไว้ = **เว็บไม่ดับแม้ deploy พัง**

## 🔍 เวลาเว็บล่ม

1. บอกคิมกดปุ่มใน Railway ทันที (`Restart` → ถ้าไม่หาย `View logs` ส่งภาพมา) — Claude เข้า Railway เองไม่ได้
2. อย่า revert แล้วรอเฉยๆ — ถ้า revert แล้วยังดับ แปลว่าต้นเหตุไม่ใช่โค้ดที่เพิ่งขึ้น
3. รันที่เครื่องตัวเองเพื่อแยกให้ออกว่าพังตอนโหลดไฟล์หรือตอนต่อ DB:
   `DATABASE_URL='postgres://x:x@127.0.0.1:1/x' PORT=3999 node -e "import('./server/index.js')"`
